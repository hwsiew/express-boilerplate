version: "3.7"

services: 
  express:
    build: 
      args: 
        NODE_ENV: development
    command: npm run start:dev
    environment: 
      - NODE_ENV='development'
    volumes:
      - ./server:/app/server